import { RoleOptionsDirective } from './role-options.directive';

describe('RoleOptionsDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleOptionsDirective();
    expect(directive).toBeTruthy();
  });
});
