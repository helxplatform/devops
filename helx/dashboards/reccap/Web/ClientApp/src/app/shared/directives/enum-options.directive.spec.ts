import { EnumOptionsDirective } from './enum-options.directive';

describe('EnumOptionsDirective', () => {
  it('should create an instance', () => {
    const directive = new EnumOptionsDirective();
    expect(directive).toBeTruthy();
  });
});
