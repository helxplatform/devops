import { Menu } from '@core/models/menu';
import { Permissions } from '@core/models/permissions.enum';

export const MenuItems: Array<Menu> = [
  {
    url: '/dashboard',
    name: 'Dashboard',
    type: 'link',
    icon: 'fa-home-alt',
  },
];
